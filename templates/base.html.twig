<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
            <link rel="stylesheet" href="{{asset('css/app.css')}}">
            <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        {% include 'header.html.twig' %}
        {% block body %}
        {% endblock %}
        <button>
    <a href="{{path('test')}}" title="vers Test">Vers Test</a>
</button>
<button>
    <a href="{{path('chart')}}" title="vers Chart">Vers Chart</a>
</button>
<button>
    <a href="{{path('redir')}}" title="vers Redir">Vers Redir</a>
</button>
<button>
    <a href="{{path('liste')}}" title="vers Redir">Vers la liste des produits</a>
</button>
        {% include 'footer.html.twig' %}
    </body>
</html>
